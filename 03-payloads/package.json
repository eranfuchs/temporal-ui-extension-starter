{
    "name": "temporal-ui-starter-03-payloads",
    "version": "0.7.3",
    "private": true,
    "type": "module",
    "description": "Everything 02 does, plus each workflow's input and result on hover — decoded through a codec server you name yourself. The rung where payloads, an outbound host and the personal-data question all arrive together. One permission: storage.",
    "license": "MIT",
    "engines": {
        "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
    },
    "scripts": {
        "build": "node esbuild.mjs",
        "watch": "node esbuild.mjs --watch",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "package": "node ../scripts/package.mjs ."
    },
    "devDependencies": {
        "@types/chrome": "^0.2.7",
        "esbuild": "^0.28.2",
        "jsdom": "^30.0.1",
        "typescript": "^5.9.3",
        "vitest": "^4.1.11"
    },
    "dependencies": {
        "p-limit": "^7.3.2",
        "valibot": "^1.4.2"
    }
}

{
    "name": "temporal-ui-starter-01-family-tree",
    "version": "0.2.5",
    "private": true,
    "type": "module",
    "description": "The smallest useful thing: parent/child workflow families in the Temporal UI, with zero permissions.",
    "license": "MIT",
    "engines": {
        "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
    },
    "scripts": {
        "build": "node esbuild.mjs",
        "watch": "node esbuild.mjs --watch",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "package": "node ../scripts/package.mjs ."
    },
    "devDependencies": {
        "esbuild": "^0.28.2",
        "jsdom": "^30.0.1",
        "typescript": "^5.9.3",
        "vitest": "^4.1.11"
    }
}
